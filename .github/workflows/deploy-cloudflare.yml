name: Deploy Website to Cloudflare

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cloudflare-production
  cancel-in-progress: true

jobs:
  deploy:
    name: Deploy to Cloudflare Workers
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 1

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          package-manager-cache: false

      - name: Validate static site boundary
        shell: bash
        run: |
          set -euo pipefail
          required=(
            wrangler.jsonc
            site/index.html site/404.html site/downloads.html site/developers.html site/about.html site/faq.html
            site/_headers site/robots.txt site/sitemap.xml
            site/css/variables.css site/css/main.css site/css/responsive.css
            site/js/main.js site/js/github-api.js site/js/evolution.js
            site/data/evolution.json site/assets/icons/favicon.svg
          )
          for file in "${required[@]}"; do
            test -f "$file" || { echo "::error::Missing required deployment file: $file"; exit 1; }
          done

      - name: Verify Wrangler configuration
        shell: bash
        run: |
          set -euo pipefail
          grep -q '"name": "voidone-website"' wrangler.jsonc
          grep -q '"directory": "./site"' wrangler.jsonc
          grep -q '"compatibility_date": "2026-09-05"' wrangler.jsonc

      - name: Verify current site sync
        shell: bash
        run: |
          set -euo pipefail
          node tests/site-sync-contract.js
          node tests/seo-contract.js

      - name: Deploy static site with pinned Wrangler
        shell: bash
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          set -euo pipefail
          npm exec --yes --package=wrangler@4.129.0 -- \
            wrangler deploy --config ./wrangler.jsonc
